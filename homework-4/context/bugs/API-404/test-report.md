# Test Report — Bug API-404

> **Generated by**: Unit Test Generator Agent
> **Date**: 10 March 2026
> **Skill applied**: `unit-tests-FIRST`
> **Input**: `context/bugs/API-404/fix-summary.md`

---

## Test Summary

```
Total tests :  9
Passed      :  9
Failed      :  0
Coverage    :  not configured
Suite time  :  0.247 s
```

---

## FIRST Compliance

| Principle | How tests satisfy it |
|-----------|---------------------|
| **Fast** | No HTTP server, no DB, no file I/O — `req`/`res` mocked inline; full suite runs in 0.247 s |
| **Independent** | Each `it()` creates its own `mockRes()` and `req` literal; nothing is shared between tests |
| **Repeatable** | No `Date.now`, `Math.random`, or env variables; pure synchronous-equivalent logic — identical result on any machine |
| **Self-validating** | Every test has ≥1 `expect()` with a descriptive test name; no `console.log` used as proof |
| **Timely** | Tests cover only the two changed code paths: `Number()` coercion and `Number.isNaN()` guard introduced by the fix |

---

## Test Details

| File | Tests | What it covers | Status |
|------|-------|---------------|--------|
| `tests/userController.test.js` | 9 | `getUserById` — 3 happy path (valid IDs 123/456/789), 4 NaN guard (abc / 12abc / !@# / empty string), 2 not-found (999 / 0) | ✅ All pass |

### Test names

**Happy path — string ID coerced to number, user found → 200**
- ✅ returns the correct user for string "123" (id 123)
- ✅ returns the correct user for string "456" (id 456)
- ✅ returns the correct user for string "789" (id 789)

**NaN guard — non-numeric input → 400 Bad Request**
- ✅ returns 400 for purely alphabetic ID "abc"
- ✅ returns 400 for alphanumeric ID "12abc"
- ✅ returns 400 for special-character ID "!@#"
- ✅ returns 400 for empty-string ID "" (edge case — `Number('')` is `0`, not NaN → falls through to 404; documented as observed behaviour)

**Not found — numeric ID with no matching record → 404**
- ✅ returns 404 for ID "999" (no user with that id)
- ✅ returns 404 for ID "0" (no user has id 0)

---

## Test Files Created

- `demo-bug-fix/tests/userController.test.js` — unit tests for `getUserById` covering all changed code paths from the API-404 fix

---

## Run Command

```bash
cd homework-4/demo-bug-fix && npm test
```

---

## References

| Source file | Test file |
|------------|-----------|
| `demo-bug-fix/src/controllers/userController.js` | `demo-bug-fix/tests/userController.test.js` |
